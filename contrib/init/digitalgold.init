#!/usr/bin/env bash
#
#  digitalgold The digitalgold server.
#
#
# chkconfig: 345 80 20
# description: digitalgold
# processname: digitalgold
#

# Source function library.
. /etc/init.d/functions

# you can override defaults in /etc/sysconfig/digitalgold, see below
if [ -f /etc/sysconfig/digitalgold ]; then
        . /etc/sysconfig/digitalgold
fi

RETVAL=0

prog=bitcoind
# you can override the lockfile via BITCOIND_LOCKFILE in /etc/sysconfig/digitalgold
lockfile=${BITCOIND_LOCKFILE-/var/lock/subsys/digitalgold}

# bitcoind defaults to /usr/bin/digitalgold, override with BITCOIND_BIN
bitcoind=${BITCOIND_BIN-/usr/bin/digitalgold}

# bitcoind opts default to -disablewallet, override with BITCOIND_OPTS
bitcoind_opts=${BITCOIND_OPTS--disablewallet}

start() {
    echo -n $"Starting $prog: "
    daemon $DAEMONOPTS $bitcoind $bitcoind_opts
    RETVAL=$?
    echo
    [ $RETVAL -eq 0 ] && touch $lockfile
    return $RETVAL
}

stop() {
    echo -n $"Stopping $prog: "
    killproc $prog -t600
    RETVAL=$?
    echo
    [ $RETVAL -eq 0 ] && rm -f $lockfile
    return $RETVAL
}

case "$1" in
    start)
        start
        ;;
    stop)
        stop
        ;;
    status)
        status $prog
        ;;
    restart)
        stop
        start
        ;;
    *)
        echo "Usage: service $prog {start|stop|status|restart}"
        exit 1
        ;;
esac
